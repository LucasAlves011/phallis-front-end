version: '3'
services:
  app:
    build:
      context: .
      args:
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
    restart: always
    ports:
      - "3100:3000"
    environment:
      - NODE_ENV=production
      - API_URL=${NEXT_PUBLIC_API_URL}
